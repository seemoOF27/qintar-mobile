/**
 * عنوان الخادم.
 *
 * **يأتي من متغير بيئة لا من ثابت في الشيفرة**: نفس الحزمة تُبنى للتجربة
 * المحلية وللنشر، ولا يُسلَّم ملف فيه عنوان الجهاز المنزلي.
 *
 * والقيمة الافتراضية عمدًا **بلا عنوان**: تطبيق يشير لخادم خاطئ صامتًا أسوأ
 * من تطبيق يقول إن إعداده ناقص.
 */
const url = process.env.EXPO_PUBLIC_API_URL ?? ''

export const config = {
  apiUrl: url.replace(/\/+$/, ''),
  isConfigured: url !== '',
} as const
